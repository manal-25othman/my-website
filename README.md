# منال عثمان — Portfolio

الموقع الشخصي الرسمي لـ **منال عثمان**، مهندسة ذكاء اصطناعي توليدي.
Portfolio ثنائي اللغة (عربي RTL + إنجليزي LTR) مبني حول المشاريع ودراسات
الحالة، لا حول السيرة الذاتية.

**مبني بلا أي اعتماديات خارجية** — مولّد ثابت (Static Site Generator) مكتوب
بـ Node خالص، ينتج HTML و CSS و JS جاهزًا للنشر.

| | |
|---|---|
| الصفحات | ١٨ صفحة × لغتين = ٣٦ صفحة |
| حجم الصفحة الرئيسية | ‎~30 KB مضغوطة (HTML + CSS + JS) |
| الاعتماديات | صفر — لا npm install، لا حزم |
| الخطوط | Tajawal مستضاف محليًا (100 KB، بلا أي طلب خارجي) |
| اللغات | العربية (الجذر) · English (‎/en/‎) |
| النشر | Vercel — تلقائي عند كل دفعة |
| لوحة التحكم | [Pages CMS](https://app.pagescms.org) |

---

## 🗺️ بنية الصفحة الرئيسية

مرتّبة لمسؤول التوظيف: **الدليل قبل السيرة**.

```
1  الهيرو — من هي منال وماذا تبني
2  شريط الإثبات — 20K+ · Generative AI · AI Products · 6+ سنوات
3  المشروع الأبرز — معرفة AI كدراسة حالة كاملة
4  مشاريع مختارة
5  ماذا أفعل
6  المهارات التقنية
7  لماذا منال؟ — موجّه لفرق التوظيف
8  من أنا
9  المحتوى التقني — 20K+
10 المنشورات
11 التعليم والشهادات
12 إلى أين أتجه
13 تواصل معي
```

---

## ⚡ البدء

```bash
npm run dev      # بناء + معاينة على http://localhost:4321
npm run build    # بناء الموقع في مجلد dist/
npm run check    # تقرير: ما الذي لم تستبدله بعد؟
```

لا حاجة لـ `npm install` — المشروع بلا اعتماديات (Node 18+ فقط).

---

## ✅ ما تبقّى قبل الإطلاق

الموقع معبّأ بالكامل بمحتوى حقيقي مأخوذ من السيرة الذاتية ومن النصوص التي
كتبتِها. المتبقي روابط وملفات فقط:

| العنصر | أين | الأثر |
|---|---|---|
| **الصورة الشخصية** | `src/assets/img/profile.png` | تظهر حاليًا كعنصر نائب `[YOUR PHOTO]` |
| **رابط TikTok** | لوحة التحكم → الهوية → الحسابات | زر «شاهد محتواي» معطّل حاليًا |
| **رابط LinkedIn** | لوحة التحكم → الهوية → الحسابات | الأيقونة معطّلة حاليًا |
| **رابط مقال واكب** | لوحة التحكم → المنشورات | زر «اقرأ المقال» معطّل حاليًا |
| **رابط/تفاصيل الإصدار** | لوحة التحكم → المنشورات | زر «التفاصيل» معطّل حاليًا |
| **السيرة الذاتية PDF** | `src/assets/files/` ثم اربطيها | زر «تحميل السيرة الذاتية» معطّل حاليًا |
| **روابط المشاريع** | لوحة التحكم → المشاريع → رابط المشروع | لا يظهر زر «زيارة المشروع» بدونها |

شغّلي `npm run check` لعرض ما تبقّى من عناصر نائبة.

> **ملاحظة على سنوات المشاريع:** تُركت فارغة عمدًا لأن السيرة الذاتية لا
> تحدّد سنة كل مشروع. أضيفيها من لوحة التحكم متى شئتِ.

---

## 📝 كيف تضيفين محتوى جديدًا

### مشروع جديد

لوحة التحكم → **المشاريع → +**، أو انسخي كائنًا في `content/projects.json`.

كل مشروع دراسة حالة: المشكلة ← الحل ← دورك ← أبرز ما عملتِ عليه ←
التقنيات ← النتيجة. الحقل `metrics` هو ما يلتقطه مسؤول التوظيف أولًا —
أرقام حقيقية فقط.

`featured: true` يجعل المشروع دراسة الحالة البارزة في الصفحة الرئيسية —
لمشروع واحد فقط.

### منشور جديد

لوحة التحكم → **المنشورات**. أضيفي العنوان وجهة النشر والوصف والرابط.

### خبرة أو شهادة أو محاضرة

لوحة التحكم → **الخبرة المهنية** أو **من أنا**. الترتيب في القائمة هو
ترتيب العرض.

---

## 🚀 النشر

### Vercel (جاهز)

المشروع مضبوط بالكامل عبر `vercel.json` — لا حاجة لإدخال أي إعداد يدوي:

| الإعداد | القيمة |
|---|---|
| Build Command | `node build.js` |
| Output Directory | `dist` |
| Install Command | متخطّى (لا اعتماديات) |
| Framework Preset | `Other` |

**الخطوات:**

1. [vercel.com/new](https://vercel.com/new) → **Import Git Repository** → اختر المستودع
2. اترك كل الحقول كما هي (Vercel يقرأ `vercel.json` تلقائياً) → **Deploy**

**الدومين يُضبط تلقائياً:** يقرأ البناء متغيّر `VERCEL_PROJECT_PRODUCTION_URL`
ويستخدمه في `canonical` و `sitemap.xml` و Open Graph، فلا تحتاج لتعديل
`content/site.json` بعد النشر.

**دومين خاص؟** بعد ربطه في Vercel، أضف متغيّر بيئة في إعدادات المشروع:

```
SITE_URL = https://yourdomain.com
```

ثم أعد النشر (Redeploy) لتلتقطه بيانات SEO.

`vercel.json` يضبط أيضاً: `trailingSlash` ليطابق روابط الموقع، وترويسات
أمان (`nosniff` · `Referrer-Policy` · `HSTS` · `Permissions-Policy`)،
وتخزيناً مؤقتاً طويلاً للخطوط ومتوسطاً للصور، مع `must-revalidate` لملفات
CSS و JS حتى يصل أي تحديث فوراً.

### أي استضافة أخرى

`npm run build` ثم ارفع محتويات `dist/` — يعمل على Netlify و Vercel و
Cloudflare Pages وأي استضافة ملفات ثابتة.

### متغيرات وقت البناء

```bash
SITE_URL=https://yourdomain.com BASE_PATH= GA_ID=G-XXXXXXX npm run build
```

| المتغير | الغرض |
|---|---|
| `SITE_URL` | يتجاوز الدومين المضبوط في `content/site.json` |
| `BASE_PATH` | مسار فرعي للاستضافة |
| `GA_ID` | معرّف Google Analytics |
| `TIKTOK_PIXEL_ID` | معرّف TikTok Pixel |

---

## 🎛️ لوحة التحكم

المحتوى مخزَّن بصيغة JSON و Markdown، ويُدار عبر
**[Pages CMS](https://app.pagescms.org)** — لوحة تحكم مجانية تعمل مباشرة
على المستودع، بلا خادم ولا قاعدة بيانات ولا تكلفة.

**الدخول:**

1. افتحي [app.pagescms.org](https://app.pagescms.org)
2. **Sign in with GitHub** → امنحي الوصول لهذا المستودع
3. اختاري المستودع → تظهر الأقسام: الهوية · المشاريع · من أنا ·
   الخبرة المهنية · المهارات · المنشورات · المحتوى التقني · الخصوصية والشروط

كل حقل له عنوان عربي وشرح مختصر. الحفظ يكتب مباشرة في المستودع، وينشر
Vercel التعديل تلقائياً خلال دقيقة.

ملف الإعداد `.pages.yml` يصف الحقول — عدّليه فقط عند إضافة حقول جديدة.

> **بديل بلا طرف ثالث:** يمكن تعديل ملفات `content/*.json` مباشرة من واجهة
> GitHub (أيقونة القلم ✏️) — يعمل من الجوال ولا يحتاج أي إعداد.

---

## 🔍 SEO والهوية الرقمية

يُولَّد تلقائياً في كل صفحة:

- عنوان ووصف فريدان · `canonical` · `hreflang` للغتين + `x-default`
- Open Graph و Twitter/X Cards
- `sitemap.xml` (مع بدائل اللغة) و `robots.txt`
- **Schema.org**: `Person` · `WebSite` · `ProfilePage` · `Article` ·
  `Book` · `SoftwareApplication` · `BreadcrumbList` · `CollectionPage` · `ContactPage`

**قاعدة صارمة مطبَّقة في الكود:** لا يدخل أي حقل في البيانات المنظمة إن كانت
قيمته فارغة أو ما زالت عنصراً نائباً. و `Book Schema` لا يُنتَج إلا إذا كان
`published: true`، و `Review` لا يُنتَج بلا مراجعة حقيقية — حتى لا يُرسَل أي
إشارة غير دقيقة إلى محركات البحث.

### بعد النشر

1. أضف الموقع إلى [Google Search Console](https://search.google.com/search-console)
   وضع كود التحقق في `seo.googleSiteVerification`.
2. أرسل `sitemap.xml`.
3. تأكد أن الرابط نفسه موضوع في نبذة كل حساب (TikTok · LinkedIn · X) —
   الربط في الاتجاهين هو ما يثبّت الهوية.

---

## 📊 التحليلات

**لا يُحمَّل أي سكربت تتبّع ما لم تضع المعرّفات بنفسك** في
`content/site.json → analytics`. عند التفعيل تُتتبَّع الأحداث التالية تلقائياً:

`page_view` · `article_view` · `tool_click` · `tiktok_click` ·
`social_click` · `contact_submit`

ويُحترم إعداد Do Not Track في المتصفح افتراضياً (`respectDoNotTrack: true`).

---

## ✉️ نموذج التواصل

الموقع ثابت — لا يوجد خادم. الوضع الافتراضي `mailto` يفتح بريد الزائر مع
تعبئة الرسالة، ويعمل فوراً بلا أي خدمة خارجية.

لاستقبال الرسائل في بريدك مباشرة، استخدم خدمة مثل Formspree أو Web3Forms:

```js
contactForm: { mode: 'endpoint', endpoint: 'https://formspree.io/f/xxxxxxx' }
```

**الحماية من السبام:** حقل خفي (honeypot) + تأخير الإرسال الفوري +
تحقق من صحة المدخلات وتحديد أطوالها قبل الإرسال.

---

## 🎨 التخصيص

**الألوان والخطوط والمقاييس** كلها متغيرات CSS في أعلى
`src/assets/css/styles.css`:

```css
:root {
  --accent: #2f6feb;      /* لون التمييز الأساسي */
  --accent-2: #7c5cf0;    /* اللون الثانوي للتدرجات الخفيفة */
  --wrap: 1200px;         /* أقصى عرض للمحتوى */
  --sec-y: clamp(3.75rem, 8vw, 7rem);
}
```

تغيير `--accent` وحده يعيد صياغة هوية الموقع بالكامل.
الوضع الداكن يعمل تلقائياً حسب إعداد النظام، مع زر تبديل يدوي يُحفظ محلياً.

---

## 🧱 بنية المشروع

```
content/            ← كل ما تعدّله أنت (عبر لوحة التحكم أو مباشرة)
  README.md           دليل المحتوى والقواعد
  site.json           الهوية المركزية (الاسم، الصورة، الحسابات، SEO، التحليلات)
  projects.json       المشاريع ودراسات الحالة
  about.json  experience.json  expertise.json
  publications.json  tiktok.json  legal.json
  ui.js               نصوص الواجهة بالعربية والإنجليزية

src/
  lib/                html · markdown · seo · icons · png (بلا اعتماديات)
  templates/          layout · components · sections · pages
  assets/             css · js · fonts · img

build.js            المولّد
serve.js            خادم المعاينة المحلي
.pages.yml          إعداد لوحة التحكم
vercel.json         إعداد النشر
dist/               الناتج (لا يُرفع إلى Git — يُبنى تلقائياً)
```

---

## 🔒 قواعد المصداقية المطبَّقة في الكود

هذا الموقع يمثّل هوية مهنية حقيقية، ولذلك:

- **لا أرقام مُختلَقة.** أي قيمة `null` تظهر كـ «—» ولا تُملأ تلقائياً.
- **الإحصائيات تُحسب من المحتوى الحقيقي فقط** — المحتوى النموذجي لا يُحتسب.
- **لا مراجعات ولا تقييمات وهمية.** بلا تقييم ⇒ «لم تُقيَّم بعد».
- **لا صور مصغّرة وهمية لمقاطع TikTok.** تُعرض المقاطع ذات الروابط الحقيقية فقط.
- **المحتوى النموذجي لا يُفهرَس** (`noindex`) ولا يدخل في `sitemap.xml`.
- **بيانات Schema.org لا تحتوي على أي عنصر نائب.**

راجع `npm run check` قبل كل نشر.

---

## ♿ إمكانية الوصول

تسلسل عناوين سليم · نصوص بديلة للصور · تنقل كامل بلوحة المفاتيح · حصر التركيز
داخل القائمة الجانبية · حالات تركيز واضحة · دعم `prefers-reduced-motion` ·
دعم كامل لـ RTL و LTR عبر الخصائص المنطقية (logical properties) ·
المحتوى يظهر كاملاً حتى مع تعطيل JavaScript.

---

## 🖨️ الملف التعريفي (Media Kit)

صفحة `/media-kit/` مهيّأة للطباعة: زر «تحميل Media Kit» يفتح نافذة الطباعة،
ومنها «حفظ بصيغة PDF» فتحصل على ملف تعريفي نظيف بلا قوائم أو أزرار.
